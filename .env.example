NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional — enables AI-assisted suggestions (activity name matching, full
# name splitting) in the roster Excel import. Suggestion-only: the app
# functions identically (manual correction UI) without it. Never exposed to
# the client, only read server-side.
OPENAI_API_KEY=
